CREATE TABLE webservices (
  id_webservices  INTEGER PRIMARY KEY AUTOINCREMENT,
  name_webservices varchar(32) not NULL,
  file_webservices varchar(255) NOT NULL,
  class_webservices varchar(255) NOT NULL
);
